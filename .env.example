DB_HOST=localhost
DB_NAME=almacensekai
# IMPORTANTE: en producción usa un usuario dedicado con contraseña fuerte, nunca root sin contraseña.
DB_USER=root
DB_PASSWORD=
DB_CHARSET=utf8mb4

# Origen permitido para CORS. En producción pon la URL exacta del frontend (sin barra final).
# Ejemplo producción: ALLOWED_ORIGIN=https://miapp.com
ALLOWED_ORIGIN=http://localhost:5173,https://appassets.androidplatform.net
